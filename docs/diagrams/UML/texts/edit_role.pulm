@startuml edit_role
start
:<Авторизация как HR>;
:<Ввод ФИО нужного человека>;
:Получение из БД данных аккаунта по ФИО]
if (Такой ФИО есть в системе?) then (Да)
  :<Вывод текущей должности человека>;
  :<Ввод новой должности>;
  if (Такая должность есть в системе?) then (Да)
    :Изменение в БД должности аккаунта]
    :<Вывод успеха>;
  else (Нет)
    :<Вывод ошибки: неизвестная должность>;
  endif
else (Нет)
  :<Вывод ошибки: неизвестный человек>;
endif
stop
@enduml